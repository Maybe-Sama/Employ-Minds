# Employ-Minds repository instructions

This repository *is* the harness, so changes must preserve its central invariant: one router, risk-adaptive ceremony, specialist roles, and fresh evidence before completion.

For non-trivial work, read `skills/employ-minds-router/SKILL.md` first. Do not make FAST bureaucratic and do not weaken CRITICAL gates. Keep Claude and Codex installation behavior semantically aligned. Run the unit suite and `python scripts/doctor.py --self-check` after the final change.
